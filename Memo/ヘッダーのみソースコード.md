<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RRDAC Pure Interface Header - Pulse Edition</title>
    <style>
        :root {
            --deep-navy: #0a0e1a;
            --accent-cyan: #5ce1e6;
            --text-white: #ffffff;
            --font-main: "Helvetica Neue", "Arial", "Hiragino Kaku Gothic ProN", sans-serif;
            --transition-smooth: all 0.6s cubic-bezier(0.23, 1, 0.32, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body, html {
            width: 100%;
            height: 100%;
            background: transparent;
            font-family: var(--font-main);
            color: var(--text-white);
            overflow: hidden;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* メインコンテナ：ノイズのない滑らかなフロストガラス */
        .header-outer {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 50px;
            background: rgba(10, 14, 26, 0.85);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(92, 225, 230, 0.15);
            position: relative;
        }

        /* ロゴ：呼吸する光（グラデーションシマー） */
        .logo-group {
            position: relative;
            cursor: pointer;
            z-index: 10;
        }

        .logo-group h1 {
            font-size: 26px;
            letter-spacing: 0.4em;
            font-weight: 200;
            text-transform: uppercase;
            margin-right: -0.4em;
            transition: var(--transition-smooth);
            background: linear-gradient(90deg, #fff, var(--accent-cyan), #fff);
            background-size: 200% auto;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: shine 10s linear infinite;
        }

        @keyframes shine {
            to { background-position: 200% center; }
        }

        .logo-group span {
            font-size: 8px;
            color: var(--accent-cyan);
            letter-spacing: 0.3em;
            text-transform: uppercase;
            margin-top: 6px;
            display: block;
            opacity: 0.7;
            font-weight: 400;
        }

        /* ナビゲーション */
        .nav-container {
            display: flex;
            align-items: center;
            gap: 60px;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 45px;
        }

        nav a {
            text-decoration: none;
            color: var(--text-white);
            font-size: 11px;
            letter-spacing: 0.25em;
            text-transform: uppercase;
            font-weight: 300;
            opacity: 0.5;
            transition: var(--transition-smooth);
            position: relative;
            padding: 8px 0;
        }

        /* ホバーライン */
        nav a::before {
            content: '';
            position: absolute;
            bottom: 0;
            left: 50%;
            width: 0;
            height: 1px;
            background: var(--accent-cyan);
            transition: var(--transition-smooth);
            transform: translateX(-50%);
            box-shadow: 0 0 10px var(--accent-cyan);
        }

        nav a:hover {
            opacity: 1;
            transform: translateY(-2px);
            letter-spacing: 0.35em;
        }

        nav a:hover::before {
            width: 100%;
        }

        /* 右端の点滅インジケーター（Pulse Dot） */
        .status-pulse {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 20px;
            height: 20px;
        }

        .dot {
            width: 4px;
            height: 4px;
            background: var(--accent-cyan);
            border-radius: 50%;
            position: relative;
            box-shadow: 0 0 10px var(--accent-cyan);
        }

        .dot::after {
            content: '';
            position: absolute;
            top: -8px;
            left: -8px;
            right: -8px;
            bottom: -8px;
            border-radius: 50%;
            border: 1px solid var(--accent-cyan);
            opacity: 0;
            animation: pulse-ring 2s cubic-bezier(0.215, 0.61, 0.355, 1) infinite;
        }

        @keyframes pulse-ring {
            0% { transform: scale(0.3); opacity: 0; }
            50% { opacity: 0.5; }
            100% { transform: scale(1.2); opacity: 0; }
        }

        /* マグネティック効果用 */
        .mag-wrap {
            display: inline-block;
            transition: transform 0.3s cubic-bezier(0.215, 0.61, 0.355, 1);
        }

        @media (max-width: 768px) {
            .header-outer { padding: 0 25px; }
            nav ul { display: none; }
            .logo-group h1 { font-size: 20px; }
        }
    </style>
</head>
<body>

    <div class="header-outer">
        <div class="logo-group mag-wrap">
            <h1>RRDAC</h1>
            <span>Root & Ray Draconia Agape Core</span>
        </div>

        <div class="nav-container">
            <nav>
                <ul>
                    <li><a href="#blueprint" target="_parent" class="mag-wrap">Blueprint</a></li>
                    <li><a href="#about" target="_parent" class="mag-wrap">About</a></li>
                    <li><a href="#contact" target="_parent" class="mag-wrap">Contact</a></li>
                </ul>
            </nav>

            <div class="status-pulse">
                <div class="dot"></div>
            </div>
        </div>
    </div>

    <script>
        const mags = document.querySelectorAll('.mag-wrap');
        
        mags.forEach((m) => {
            m.addEventListener('mousemove', (e) => {
                const r = m.getBoundingClientRect();
                const x = e.clientX - r.left - r.width / 2;
                const y = e.clientY - r.top - r.height / 2;
                m.style.transform = `translate(${x * 0.2}px, ${y * 0.2}px)`;
            });
            
            m.addEventListener('mouseleave', () => {
                m.style.transform = `translate(0px, 0px)`;
            });
        });
    </script>
</body>
</html>